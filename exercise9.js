// Function that takes two strings as input and checks if they are equal. Return true if they are equal, false otherwise

function equalStrings(str1, str2) {
    return str1 === str2;
}

console.log(equalStrings('apple', 'apple')); // Output: true
console.log(equalStrings('orange', 'banana')); // Output: false