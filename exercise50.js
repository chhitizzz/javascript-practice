// Function that takes a base and an exponent and returns the result of raising the base to the exponent

function raisedPower(base, exponent) {
    return base ** exponent;
}

result = raisedPower(2, 2);
console.log(result);